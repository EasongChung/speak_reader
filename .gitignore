# 编译产物
build/
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies

# Android
android/.gradle/
android/local.properties
android/app/debug/
android/app/profile/
android/app/release/
*.apk
# [v2.5.0] 锁文件不入库(CI 每次 flutter pub get 重新解析, 依赖浮动由 pubspec.yaml 精确版本控制)
pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# Tesseract 训练数据(由 CI 下载,不提交)
*.traineddata

# Python 缓存(CI 辅助脚本运行时产生)
__pycache__/
*.pyc
